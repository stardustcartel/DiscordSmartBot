{
  "name": "discord-smart-bot",
  "version": "0.1.0",
  "private": true,
  "description": "A configurable Gemini-powered Discord bot for independently hosted customer instances",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/storage.js && node --check src/knowledge.js && node --check src/reminders.js && node --check src/ai.js && node --check scripts/provision-instance.js",
    "provision": "node scripts/provision-instance.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@google/genai": "^1.52.0",
    "discord.js": "^14.21.0",
    "dotenv": "^17.2.1"
  }
}
